# MCQ-Generator

```
1. run the command to create a virtual environmemnt
python -m venv venv

2. activate venv
venv\Scripts\activate

3. install requirements
pip install -r requirements.txt

4. to check all the packages which are installed in the venv
pip list

5. install ipykernel
pip install ipykernel
```